---
phase: 04-search-filter
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - Z-Paste/Views/MainWindow/CardListView.swift
  - Z-Paste/Views/MainWindow/ClipboardCardView.swift
  - Z-Paste/Views/MainWindow/Previews/TextPreview.swift
  - Z-Paste/Views/MainWindow/SearchErrorStateView.swift
  - Tests/Z-PasteTests/SearchFilterTests.swift
autonomous: false
requirements:
  - FR-4.2
  - FR-4.3
must_haves:
  truths:
    - "No-results state is distinct from empty-history state and keeps current query/filter visible."
    - "Search error state shows Unable to Load and recovery happens on next open or search/filter change."
    - "Clear Search is the primary recovery action when a query exists."
    - "Text and RTF cards show match highlight; image and file cards do not."
  artifacts:
    - path: "Z-Paste/Views/MainWindow/CardListView.swift"
      provides: "No-results and error state branching"
      contains: "SearchErrorStateView"
    - path: "Z-Paste/Views/MainWindow/SearchErrorStateView.swift"
      provides: "Unable to Load error composition"
      contains: "Unable to load search results. Please try again."
    - path: "Z-Paste/Views/MainWindow/Previews/TextPreview.swift"
      provides: "Text highlight rendering"
      contains: "highlightedContent"
    - path: "Z-Paste/Views/MainWindow/ClipboardCardView.swift"
      provides: "Passes searchQuery into text preview"
      contains: "TextPreview(content: item.content, searchQuery: searchQuery)"
    - path: "Tests/Z-PasteTests/SearchFilterTests.swift"
      provides: "Highlight + no-results tests"
      contains: "testHighlightBuildsAttributedStringForMatchingText"
  key_links:
    - from: "Z-Paste/Views/MainWindow/CardListView.swift"
      to: "Z-Paste/ViewModels/ClipboardViewModel.swift"
      via: "Branches on hasClipboardHistory/isFilteredEmpty/searchError"
      pattern: "viewModel\\.(isFilteredEmpty|searchError)"
    - from: "Z-Paste/Views/MainWindow/CardListView.swift"
      to: "Z-Paste/Views/MainWindow/SearchErrorStateView.swift"
      via: "Renders error state view"
      pattern: "SearchErrorStateView\\("
    - from: "Z-Paste/Views/MainWindow/ClipboardCardView.swift"
      to: "Z-Paste/Views/MainWindow/Previews/TextPreview.swift"
      via: "Passes searchQuery to enable highlight"
      pattern: "TextPreview\\(content: item\\.content, searchQuery:"
---

<objective>
完成 Phase 4 的结果反馈闭环：区分无历史与无结果、提供 Clear Search 主恢复动作，补齐搜索失败态，并为文本/RTF 卡片添加搜索高亮。

Purpose: 满足 FR-4.3 并确保搜索反馈语义清晰。
Output: NoResultsState、SearchErrorState、文本高亮预览、卡片到预览的 searchQuery 传递与验证。
</objective>

<execution_context>
@/Users/longzhao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/longzhao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-search-filter/04-CONTEXT.md
@.planning/phases/04-search-filter/04-RESEARCH.md
@.planning/phases/04-search-filter/04-VALIDATION.md
@.planning/phases/04-search-filter/04-UI-SPEC.md
@.planning/phases/02-main-interface/02-UI-SPEC.md
@Z-Paste/ViewModels/ClipboardViewModel.swift
@Z-Paste/Views/MainWindow/CardListView.swift
@Z-Paste/Views/MainWindow/ClipboardCardView.swift
@Z-Paste/Views/MainWindow/Previews/TextPreview.swift

<interfaces>
From Z-Paste/ViewModels/ClipboardViewModel.swift after 04-01/04-02:
```swift
@Published var items: [ClipboardItem] = []
@Published var searchQuery: String = ""
@Published var activeFilter: ClipboardSearchFilter = .all
@Published var hasClipboardHistory: Bool = false
var isFilteredEmpty: Bool { hasClipboardHistory && items.isEmpty }
func clearSearchQuery()
```

From Z-Paste/Views/MainWindow/Previews/TextPreview.swift:
```swift
struct TextPreview: View {
    let content: String
    let searchQuery: String
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为文本/RTF 预览添加搜索高亮（图片/文件不高亮）</name>
  <files>Z-Paste/Views/MainWindow/Previews/TextPreview.swift, Z-Paste/Views/MainWindow/ClipboardCardView.swift, Tests/Z-PasteTests/SearchFilterTests.swift</files>
  <read_first>
    - .planning/phases/04-search-filter/04-CONTEXT.md
    - .planning/phases/04-search-filter/04-UI-SPEC.md
    - Z-Paste/Views/MainWindow/Previews/TextPreview.swift
    - Z-Paste/Views/MainWindow/ClipboardCardView.swift
    - Tests/Z-PasteTests/SearchFilterTests.swift
  </read_first>
  <behavior>
    - Test 1: query="swift" 时，`Swift clipboard notes` 命中片段具有高亮背景。
    - Test 2: 空 query 时，文本预览不产生任何高亮区间。
    - Test 3: `.rtf` 与 `.text` 走同一高亮入口。
  </behavior>
  <action>
    修改 `TextPreview.swift`：
    - 结构签名改为 `struct TextPreview: View { let content: String; let searchQuery: String }`
    - 新增 `static func highlightedContent(_ content: String, query: String) -> AttributedString`
    - 高亮规则：对 `content.localizedStandardRange(of: query)` 的首个命中片段设置 `backgroundColor`，颜色使用系统 accent 的 18%-24% 透明度（例如 `.accentColor.opacity(0.2)`）
    - 当 `query.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty` 时返回无高亮 `AttributedString(content)`
    - 保持 `.font(.body)`, `.lineLimit(4)`, `.truncationMode(.tail)` 与 `.multilineTextAlignment(.leading)` 不变

    修改 `ClipboardCardView.swift`：
    - 新增属性 `let searchQuery: String`，构造器增加默认参数 `searchQuery: String = ""`
    - `.text` / `.rtf` 分支改为 `TextPreview(content: item.content, searchQuery: searchQuery)`

    明确禁止：
    - 不对图片卡片添加高亮或覆盖层
    - 不对文件卡片高亮文件名（文件名必须保持正常样式）
    - 不引入第三方富文本渲染库

    在 `SearchFilterTests.swift` 新增 `testHighlightBuildsAttributedStringForMatchingText()`，断言 `highlightedContent` 返回的 `AttributedString` 存在高亮属性区间。
  </action>
  <acceptance_criteria>
    - `Z-Paste/Views/MainWindow/Previews/TextPreview.swift` 包含精确字符串 `let searchQuery: String`
    - `Z-Paste/Views/MainWindow/Previews/TextPreview.swift` 包含精确字符串 `static func highlightedContent`
    - `Z-Paste/Views/MainWindow/Previews/TextPreview.swift` 包含精确字符串 `accentColor.opacity(0.2)` 或等价 18%-24% 透明度表达
    - `Z-Paste/Views/MainWindow/ClipboardCardView.swift` 包含精确字符串 `TextPreview(content: item.content, searchQuery: searchQuery)`
    - `Tests/Z-PasteTests/SearchFilterTests.swift` 包含精确字符串 `func testHighlightBuildsAttributedStringForMatchingText()`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Z-Paste -destination 'platform=macOS' -only-testing:Z-PasteTests/SearchFilterTests</automated>
  </verify>
  <done>文本与富文本预览具备高亮，图片/文件仍保持原有预览。</done>
</task>

<task type="auto">
  <name>Task 2: 区分无历史/无结果/失败态，并提供 Clear Search 恢复动作</name>
  <files>Z-Paste/Views/MainWindow/CardListView.swift, Z-Paste/Views/MainWindow/SearchErrorStateView.swift, Z-Paste/Views/MainWindow/ClipboardCardView.swift, Tests/Z-PasteTests/SearchFilterTests.swift</files>
  <read_first>
    - .planning/phases/04-search-filter/04-CONTEXT.md
    - .planning/phases/04-search-filter/04-UI-SPEC.md
    - Z-Paste/ViewModels/ClipboardViewModel.swift
    - Z-Paste/Views/MainWindow/CardListView.swift
  </read_first>
  <action>
    新增 `SearchErrorStateView.swift`（如已有相似空态组件则复用样式）：
    - 使用 SF Symbol `exclamationmark.triangle`
    - 标题：`Unable to Load`
    - 正文：`Unable to load search results. Please try again.`
    - 图标使用系统 red 或 warning orange，但不得用 accent
    - 不放置显式按钮；恢复行为通过下次面板打开或搜索/筛选变化隐式触发

    修改 `CardListView.swift` 的空态分支为四态：
    - 若存在搜索/筛选失败标记（如 `viewModel.searchError != nil` 或等价信号）：显示 `SearchErrorStateView`
    - `!viewModel.hasClipboardHistory`：显示既有 `No Clipboard History` 空状态
    - `viewModel.isFilteredEmpty`：显示专用无结果态（No Matching Results）
    - 其他：显示卡片列表

    无结果态内容必须严格遵守 UI-SPEC 文案：
    - 标题：`No Matching Results`
    - 若 `viewModel.searchQuery` 非空：正文为 `Try a different keyword or clear search.`，并显示主按钮 `Clear Search`
    - 若 `viewModel.searchQuery` 为空且 `activeFilter != .all`：正文为 `No items match this filter.`，不显示 `Clear Search`
    - 无结果态仍保留顶部搜索与筛选 UI（不隐藏，不重置）
    - `Clear Search` 按钮触发 `viewModel.clearSearchQuery()`，不得重置 `activeFilter`

    同时在构建卡片时传入 `searchQuery: viewModel.searchQuery`。
  </action>
  <acceptance_criteria>
    - `Z-Paste/Views/MainWindow/SearchErrorStateView.swift` 包含精确字符串 `exclamationmark.triangle`
    - `Z-Paste/Views/MainWindow/SearchErrorStateView.swift` 包含精确字符串 `Unable to Load`
    - `Z-Paste/Views/MainWindow/SearchErrorStateView.swift` 包含精确字符串 `Unable to load search results. Please try again.`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 包含精确字符串 `SearchErrorStateView`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 包含精确字符串 `No Matching Results`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 包含精确字符串 `Try a different keyword or clear search.`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 包含精确字符串 `No items match this filter.`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 包含精确字符串 `Clear Search`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 不包含精确字符串 `Back to All`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 包含精确字符串 `viewModel.isFilteredEmpty`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 或 `ClipboardCardView.swift` 包含精确字符串 `searchQuery: viewModel.searchQuery`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Z-Paste -destination 'platform=macOS' -only-testing:Z-PasteTests/SearchFilterTests</automated>
  </verify>
  <done>无结果态与无历史态区分明确，搜索失败态文案与图标符合 UI-SPEC，Clear Search 仅在有查询时出现且不重置筛选。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工确认无结果态、失败态与高亮展示</name>
  <files>Z-Paste/Views/MainWindow/CardListView.swift, Z-Paste/Views/MainWindow/SearchErrorStateView.swift, Z-Paste/Views/MainWindow/Previews/TextPreview.swift, Z-Paste/Views/MainWindow/ClipboardCardView.swift</files>
  <read_first>
    - .planning/phases/04-search-filter/04-VALIDATION.md
    - Z-Paste/Views/MainWindow/CardListView.swift
    - Z-Paste/Views/MainWindow/SearchErrorStateView.swift
    - Z-Paste/Views/MainWindow/Previews/TextPreview.swift
    - Z-Paste/Views/MainWindow/ClipboardCardView.swift
  </read_first>
  <action>
    运行应用，逐项验证搜索结果反馈、失败态与高亮渲染。此任务不修改代码。
  </action>
  <what-built>文本/RTF 高亮、无结果态与 Clear Search 恢复动作、搜索失败态。</what-built>
  <how-to-verify>
    1. 确保历史中有文本或富文本记录包含 `swift`。
    2. 输入 `swift`，确认命中文本/富文本卡片正文出现可见高亮。
    3. 选择图片或文件命中场景，确认卡片仍为正常预览，无高亮。
    4. 输入 `zzzz-no-match`，确认显示 `No Matching Results` 且出现 `Clear Search`。
    5. 点击 `Clear Search`，确认查询被清空且筛选保持原样。
    6. 在查询为空时切换到无匹配筛选，确认显示 `No Matching Results` + `No items match this filter.` 且不出现 `Clear Search`。
    7. 触发搜索/筛选失败（例如强制抛出错误或注入测试标记），确认显示 `Unable to Load` + `Unable to load search results. Please try again.` 且图标为 `exclamationmark.triangle`。
  </how-to-verify>
  <acceptance_criteria>
    - 文本/RTF 卡片命中片段有可见高亮背景
    - 图片/文件卡片无高亮覆盖
    - 无结果态文案为 `No Matching Results`
    - 有查询时出现 `Clear Search`，无查询时不出现
    - 点击 `Clear Search` 后筛选未被重置为 All
    - 失败态显示 `Unable to Load` 与指定正文，图标为 `exclamationmark.triangle`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Z-Paste -destination 'platform=macOS'</automated>
  </verify>
  <done>人工确认结果反馈、失败态与高亮展示符合 UI-SPEC 与锁定决策。</done>
  <resume-signal>输入“approved”继续，或描述需要修正的问题。</resume-signal>
</task>

</tasks>

<verification>
- 自动化测试覆盖高亮 helper 与无结果规则。
- 人工确认高亮可见性、无结果态与失败态恢复路径。
</verification>

<success_criteria>
- FR-4.3 搜索高亮仅作用于文本/RTF。
- 无结果态与无历史态分离，Clear Search 作为主恢复动作生效。
- 搜索失败态文案与图标符合 UI-SPEC，并通过下一次交互隐式恢复。
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-filter/04-03-SUMMARY.md`
</output>
