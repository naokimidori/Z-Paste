---
phase: 02-main-interface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Z-Paste/Views/MainWindow/ClipboardCardView.swift
  - Z-Paste/Views/MainWindow/Previews/TextPreview.swift
  - Z-Paste/Views/MainWindow/Previews/ImagePreview.swift
  - Z-Paste/Views/MainWindow/Previews/FilePreview.swift
autonomous: true
requirements: [2.3, 2.5, 2.6]
user_setup: []

must_haves:
  truths:
    - "卡片显示内容类型标签（文本/图片/文件/富文本）"
    - "卡片显示收藏星标图标"
    - "卡片显示来源应用图标、时间戳、内容大小"
    - "卡片有选中高亮边框效果"
    - "文本卡片显示多行预览"
    - "图片卡片显示缩略图"
    - "文件卡片显示图标和文件名"
  artifacts:
    - path: "Z-Paste/Views/MainWindow/ClipboardCardView.swift"
      provides: "单个剪贴板卡片视图组件"
      exports: ["ClipboardCardView"]
    - path: "Z-Paste/Views/MainWindow/Previews/TextPreview.swift"
      provides: "文本内容预览"
    - path: "Z-Paste/Views/MainWindow/Previews/ImagePreview.swift"
      provides: "图片缩略图预览"
    - path: "Z-Paste/Views/MainWindow/Previews/FilePreview.swift"
      provides: "文件信息预览"
  key_links:
    - from: "ClipboardCardView"
      to: "ClipboardItem"
      via: "传入 item 参数"
      pattern: "let item: ClipboardItem"
    - from: "ClipboardCardView"
      to: "预览组件"
      via: "switch item.itemType"
      pattern: "case .text: TextPreview"
---

<objective>
创建 ClipboardCardView 卡片组件及预览子组件

Purpose: 实现剪贴板卡片的 UI 组件，显示内容预览、类型标签、来源信息
Output: ClipboardCardView.swift + 预览组件文件
</objective>

<execution_context>
@/Users/longzhao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/longzhao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-main-interface/02-CONTEXT.md
@.planning/phases/02-main-interface/02-RESEARCH.md
@.planning/phases/02-main-interface/02-UI-SPEC.md
</context>

<interfaces>
<!-- Key types from existing codebase -->

From Z-Paste/Models/ClipboardItem.swift:
```swift
enum ItemType: String, Codable, CaseIterable {
    case text  // 纯文本
    case image // 图片
    case file  // 文件
    case rtf   // 富文本
}

struct ClipboardItem: Codable, Identifiable, Equatable, Hashable {
    var id: Int64?
    var content: String
    var itemType: ItemType
    var sourceApp: String?
    var sourceAppIcon: Data?
    var createdAt: Date
    var isFavorite: Bool
    var data: Data?  // 图片二进制数据
}
```

From UI-SPEC.md - 卡片布局:
```
┌─────────────────────────────────────┐
│ [Type Label]              [Star]    │  ← Header row (top)
│                                     │
│         Content Preview             │  ← Main content (center)
│         (text/image/file)           │
│                                     │
│ [App Icon] [Timestamp]    [Size]    │  ← Footer row (bottom)
└─────────────────────────────────────┘
```

From UI-SPEC.md - 卡片规格:
- Size: 250px × 250px (锁定)
- Corner radius: 12px
- Background: .ultraThinMaterial
- Shadow: color: .black.opacity(0.1), radius: 4, y: 2
- Selection border: 2px system accent color when selected
- Hover effect: .opacity(0.9)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建卡片预览组件</name>
  <files>
    Z-Paste/Views/MainWindow/Previews/TextPreview.swift
    Z-Paste/Views/MainWindow/Previews/ImagePreview.swift
    Z-Paste/Views/MainWindow/Previews/FilePreview.swift
  </files>
  <read_first>
    - Z-Paste/Models/ClipboardItem.swift (理解 ItemType 和数据结构)
    - .planning/phases/02-main-interface/02-UI-SPEC.md (预览组件规格)
    - .planning/phases/02-main-interface/02-CONTEXT.md (卡片设计决策)
  </read_first>
  <action>
创建三个预览组件文件：

**1. TextPreview.swift** (文本预览):
```swift
import SwiftUI

struct TextPreview: View {
    let content: String

    var body: some View {
        Text(content)
            .font(.body)
            .lineLimit(4)  // 多行预览，最多4行
            .truncationMode(.tail)
            .multilineTextAlignment(.leading)
            .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)
            .padding(.horizontal, 16)
    }
}
```

**2. ImagePreview.swift** (图片预览):
```swift
import SwiftUI

struct ImagePreview: View {
    let imageData: Data?

    var body: some View {
        if let data = imageData,
           let nsImage = NSImage(data: data) {
            Image(nsImage: nsImage)
                .resizable()
                .aspectRatio(contentMode: .fill)
                .frame(maxWidth: .infinity, maxHeight: .infinity)
                .clipped()
        } else {
            Image(systemName: "photo")
                .font(.system(size: 48))
                .foregroundColor(.secondary)
        }
    }
}
```

**3. FilePreview.swift** (文件预览):
```swift
import SwiftUI

struct FilePreview: View {
    let content: String  // 文件路径

    var body: some View {
        VStack(spacing: 8) {
            // 文件图标
            Image(systemName: fileIcon)
                .font(.system(size: 40))
                .foregroundColor(.secondary)

            // 文件名
            Text(fileName)
                .font(.caption)
                .lineLimit(2)
                .truncationMode(.middle)
                .multilineTextAlignment(.center)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }

    private var fileName: String {
        URL(fileURLWithPath: content).lastPathComponent
    }

    private var fileIcon: String {
        let ext = URL(fileURLWithPath: content).pathExtension.lowercased()
        switch ext {
        case "pdf": return "doc.text.fill"
        case "doc", "docx": return "doc.fill"
        case "xls", "xlsx": return "chart.bar.fill"
        case "zip", "rar": return "doc.zipper"
        default: return "doc.fill"
        }
    }
}
```

创建目录结构（如果不存在）:
- Z-Paste/Views/MainWindow/Previews/
</action>
  <verify>
    <automated>test -f Z-Paste/Views/MainWindow/Previews/TextPreview.swift && test -f Z-Paste/Views/MainWindow/Previews/ImagePreview.swift && test -f Z-Paste/Views/MainWindow/Previews/FilePreview.swift</automated>
  </verify>
  <acceptance_criteria>
    - Z-Paste/Views/MainWindow/Previews/TextPreview.swift 存在
    - Z-Paste/Views/MainWindow/Previews/ImagePreview.swift 存在
    - Z-Paste/Views/MainWindow/Previews/FilePreview.swift 存在
    - TextPreview 包含 `lineLimit(4)`
    - ImagePreview 包含 `NSImage(data:)` 转换
    - FilePreview 包含文件图标和文件名显示
  </acceptance_criteria>
  <done>三个预览组件创建完成，支持文本、图片、文件类型展示</done>
</task>

<task type="auto">
  <name>Task 2: 创建 ClipboardCardView 主组件</name>
  <files>Z-Paste/Views/MainWindow/ClipboardCardView.swift</files>
  <read_first>
    - Z-Paste/Models/ClipboardItem.swift (数据模型)
    - Z-Paste/Views/MainWindow/Previews/TextPreview.swift (刚创建)
    - Z-Paste/Views/MainWindow/Previews/ImagePreview.swift (刚创建)
    - Z-Paste/Views/MainWindow/Previews/FilePreview.swift (刚创建)
    - .planning/phases/02-main-interface/02-UI-SPEC.md (卡片规格)
  </read_first>
  <action>
创建 ClipboardCardView.swift，实现完整卡片布局：

```swift
import SwiftUI

struct ClipboardCardView: View {
    let item: ClipboardItem
    let isSelected: Bool
    let onTap: (() -> Void)?

    init(item: ClipboardItem, isSelected: Bool = false, onTap: (() -> Void)? = nil) {
        self.item = item
        self.isSelected = isSelected
        self.onTap = onTap
    }

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            // Header: 类型标签 + 收藏标记
            headerView

            // Content: 内容预览
            contentView
                .frame(maxHeight: .infinity)

            // Footer: 来源图标 + 时间戳 + 大小
            footerView
        }
        .frame(width: 250, height: 250)  // 锁定尺寸
        .background(.ultraThinMaterial)
        .clipShape(RoundedRectangle(cornerRadius: 12))
        .overlay(
            RoundedRectangle(cornerRadius: 12)
                .stroke(isSelected ? Color.accentColor : Color.clear, lineWidth: 2)
        )
        .shadow(color: .black.opacity(0.1), radius: 4, x: 0, y: 2)
        .onTapGesture {
            onTap?()
        }
    }

    // MARK: - Header
    private var headerView: some View {
        HStack {
            // 类型标签
            Text(typeLabel)
                .font(.caption)
                .foregroundColor(.secondary)

            Spacer()

            // 收藏标记
            if item.isFavorite {
                Image(systemName: "star.fill")
                    .font(.caption)
                    .foregroundColor(.yellow)
            }
        }
        .padding(8)
    }

    // MARK: - Content
    @ViewBuilder
    private var contentView: some View {
        switch item.itemType {
        case .text, .rtf:
            TextPreview(content: item.content)
        case .image:
            ImagePreview(imageData: item.data)
        case .file:
            FilePreview(content: item.content)
        }
    }

    // MARK: - Footer
    private var footerView: some View {
        HStack(spacing: 4) {
            // 来源应用图标
            if let iconData = item.sourceAppIcon,
               let nsImage = NSImage(data: iconData) {
                Image(nsImage: nsImage)
                    .resizable()
                    .frame(width: 16, height: 16)
            }

            // 时间戳
            Text(item.createdAt.formatted(.relative(presentation: .named)))
                .font(.caption2)
                .foregroundColor(.secondary)

            Spacer()

            // 内容大小
            Text(contentSize)
                .font(.caption2)
                .foregroundColor(.secondary)
        }
        .padding(8)
    }

    // MARK: - Helpers
    private var typeLabel: String {
        switch item.itemType {
        case .text: return "文本"
        case .image: return "图片"
        case .file: return "文件"
        case .rtf: return "富文本"
        }
    }

    private var contentSize: String {
        switch item.itemType {
        case .text, .rtf:
            return "\(item.content.count) 字符"
        case .image:
            if let data = item.data {
                return ByteCountFormatter.string(fromByteCount: Int64(data.count), countStyle: .file)
            }
            return "未知大小"
        case .file:
            // 尝试获取文件大小
            let url = URL(fileURLWithPath: item.content)
            if let attrs = try? FileManager.default.attributesOfItem(atPath: url.path),
               let size = attrs[.size] as? Int64 {
                return ByteCountFormatter.string(fromByteCount: size, countStyle: .file)
            }
            return "未知大小"
        }
    }
}
```

**关键实现要点：**
1. 卡片尺寸固定 250x250px（CONTEXT.md 锁定）
2. 圆角半径 12px
3. 背景 .ultraThinMaterial 毛玻璃效果
4. 选中时 2px 系统强调色边框
5. 阴影 .black.opacity(0.1), radius: 4, y: 2
6. 三段式布局：Header + Content + Footer
</action>
  <verify>
    <automated>grep -q "struct ClipboardCardView" Z-Paste/Views/MainWindow/ClipboardCardView.swift && grep -q "frame(width: 250, height: 250)" Z-Paste/Views/MainWindow/ClipboardCardView.swift && grep -q "isSelected" Z-Paste/Views/MainWindow/ClipboardCardView.swift</automated>
  </verify>
  <acceptance_criteria>
    - Z-Paste/Views/MainWindow/ClipboardCardView.swift 存在
    - 文件包含 `struct ClipboardCardView: View`
    - 文件包含 `let item: ClipboardItem` 参数
    - 文件包含 `let isSelected: Bool` 参数
    - 卡片尺寸固定为 `frame(width: 250, height: 250)`
    - 包含 `.ultraThinMaterial` 背景
    - 包含选中边框 `.stroke(isSelected ? Color.accentColor`
    - 包含 switch 语句处理不同 itemType
    - Footer 显示来源图标、时间戳、大小
    - Header 显示类型标签和收藏星标
  </acceptance_criteria>
  <done>ClipboardCardView 完整实现，支持三种类型内容预览、选中状态、收藏标记</done>
</task>

</tasks>

<verification>
**手动验证步骤：**
1. 创建测试用 ClipboardItem 数据
2. 在 Preview 中显示不同类型的卡片
3. 验证卡片尺寸 250x250
4. 验证选中状态边框颜色
5. 验证收藏星标显示
</verification>

<success_criteria>
- ClipboardCardView.swift 创建完成
- 三个预览组件创建完成
- 卡片尺寸正确 250x250px
- 选中高亮边框效果
- 毛玻璃背景效果
- 支持文本/图片/文件三种类型预览
</success_criteria>

<output>
After completion, create `.planning/phases/02-main-interface/02-02-SUMMARY.md`
</output>
