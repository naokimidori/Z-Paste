---
phase: 02-main-interface
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - Z-Paste/ViewModels/ClipboardViewModel.swift
  - Z-Paste/Views/MainWindow/CardListView.swift
autonomous: true
requirements: [2.4, 2.5]
user_setup: []

must_haves:
  truths:
    - "用户可以横向滚动查看所有剪贴板历史"
    - "默认选中最新卡片（最左侧）"
    - "左右箭头键可以选择相邻卡片"
    - "选中卡片有高亮边框"
    - "卡片按时间倒序排列，最新在左"
  artifacts:
    - path: "Z-Paste/ViewModels/ClipboardViewModel.swift"
      provides: "剪贴板数据绑定和状态管理"
      exports: ["ClipboardViewModel"]
    - path: "Z-Paste/Views/MainWindow/CardListView.swift"
      provides: "横向滚动卡片列表"
      exports: ["CardListView"]
  key_links:
    - from: "CardListView"
      to: "ClipboardViewModel"
      via: "@ObservedObject viewModel"
      pattern: "@ObservedObject var viewModel"
    - from: "CardListView"
      to: "ClipboardCardView"
      via: "ForEach items"
      pattern: "ClipboardCardView(item:"
    - from: "ClipboardViewModel"
      to: "DatabaseService"
      via: "fetchRecent()"
      pattern: "database.fetchRecent"
---

<objective>
创建 CardListView 横向滚动列表和 ClipboardViewModel 数据管理

Purpose: 实现剪贴板历史的横向滚动展示和键盘导航
Output: CardListView.swift + ClipboardViewModel.swift
</objective>

<execution_context>
@/Users/longzhao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/longzhao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-main-interface/02-CONTEXT.md
@.planning/phases/02-main-interface/02-RESEARCH.md
@.planning/phases/02-main-interface/02-UI-SPEC.md
</context>

<interfaces>
<!-- Key types from Wave 1 plans -->

From Z-Paste/Views/MainWindow/ClipboardCardView.swift:
```swift
struct ClipboardCardView: View {
    let item: ClipboardItem
    let isSelected: Bool
    let onTap: (() -> Void)?
}
```

From Z-Paste/Services/DatabaseService.swift:
```swift
class DatabaseService {
    func fetchRecent(limit: Int) throws -> [ClipboardItem]
}
```

From Z-Paste/Models/ClipboardItem.swift:
```swift
struct ClipboardItem: Codable, Identifiable {
    var id: Int64?
    // ... other properties
}
```

From RESEARCH.md - 模式 3: 横向滚动列表:
```swift
ScrollView(.horizontal, showsIndicators: false) {
    LazyHStack(spacing: 12) {
        ForEach(items) { item in
            ClipboardCardView(item: item, isSelected: selectedID == item.id)
        }
    }
    .padding(.horizontal, 16)
    .padding(.vertical, 12)
}
```

From RESEARCH.md - 模式 5: 键盘导航:
```swift
.onKeyPress(.leftArrow) { /* select previous */ }
.onKeyPress(.rightArrow) { /* select next */ }
.onKeyPress(.return) { /* select and copy */ }
.onKeyPress(.escape) { /* hide window */ }
```

From UI-SPEC.md - 键盘导航:
- Left Arrow: Select previous card (wrap to last if at first)
- Right Arrow: Select next card (wrap to first if at last)
- Enter: Select and copy current card
- Escape: Close window
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ClipboardViewModel</name>
  <files>Z-Paste/ViewModels/ClipboardViewModel.swift</files>
  <read_first>
    - Z-Paste/Services/DatabaseService.swift (理解数据库操作)
    - Z-Paste/Models/ClipboardItem.swift (数据模型)
    - Z-Paste/Services/ClipboardService.swift (理解 onNewItem 回调)
    - .planning/phases/02-main-interface/02-RESEARCH.md (ViewModel 示例)
  </read_first>
  <action>
创建 ClipboardViewModel.swift，实现数据管理和状态绑定：

```swift
import SwiftUI
import Combine

@MainActor
class ClipboardViewModel: ObservableObject {
    // MARK: - Published Properties
    @Published var items: [ClipboardItem] = []
    @Published var selectedIndex: Int = 0
    @Published var isLoading: Bool = false
    @Published var errorMessage: String?

    // MARK: - Dependencies
    private let database: DatabaseService
    private var cancellables = Set<AnyCancellable>()

    // MARK: - Computed Properties
    var selectedItem: ClipboardItem? {
        guard selectedIndex >= 0 && selectedIndex < items.count else { return nil }
        return items[selectedIndex]
    }

    var isEmpty: Bool {
        items.isEmpty
    }

    // MARK: - Initialization
    init(database: DatabaseService) {
        self.database = database
        loadItems()
    }

    // MARK: - Data Loading
    func loadItems() {
        isLoading = true
        errorMessage = nil

        Task {
            do {
                items = try database.fetchRecent(limit: 100)
                // 默认选中第一个（最新的）
                selectedIndex = items.isEmpty ? -1 : 0
            } catch {
                errorMessage = "无法加载剪贴板历史: \(error.localizedDescription)"
            }
            isLoading = false
        }
    }

    // MARK: - Navigation
    func selectNext() {
        guard !items.isEmpty else { return }
        if selectedIndex < items.count - 1 {
            selectedIndex += 1
        } else {
            // Wrap to first
            selectedIndex = 0
        }
    }

    func selectPrevious() {
        guard !items.isEmpty else { return }
        if selectedIndex > 0 {
            selectedIndex -= 1
        } else {
            // Wrap to last
            selectedIndex = items.count - 1
        }
    }

    func selectItem(at index: Int) {
        guard index >= 0 && index < items.count else { return }
        selectedIndex = index
    }

    func selectItem(id: Int64) {
        if let index = items.firstIndex(where: { $0.id == id }) {
            selectedIndex = index
        }
    }

    // MARK: - Actions (Phase 3 will implement)
    func copySelected() {
        guard let item = selectedItem else { return }
        // TODO: Phase 3 - 实现复制到剪贴板
        print("Copy selected: \(item.content)")
    }
}
```

创建目录（如果不存在）:
- Z-Paste/ViewModels/
</action>
  <verify>
    <automated>grep -q "class ClipboardViewModel" Z-Paste/ViewModels/ClipboardViewModel.swift && grep -q "selectNext" Z-Paste/ViewModels/ClipboardViewModel.swift && grep -q "selectPrevious" Z-Paste/ViewModels/ClipboardViewModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - Z-Paste/ViewModels/ClipboardViewModel.swift 存在
    - 文件包含 `@MainActor class ClipboardViewModel: ObservableObject`
    - 文件包含 `@Published var items: [ClipboardItem]`
    - 文件包含 `@Published var selectedIndex: Int`
    - 文件包含 `func selectNext()` 方法
    - 文件包含 `func selectPrevious()` 方法
    - 文件包含 `func loadItems()` 方法
    - 包含 wrap-around 逻辑（到最后一个后回到第一个）
  </acceptance_criteria>
  <done>ClipboardViewModel 实现完成，提供数据加载和导航方法</done>
</task>

<task type="auto">
  <name>Task 2: 创建 CardListView</name>
  <files>Z-Paste/Views/MainWindow/CardListView.swift</files>
  <read_first>
    - Z-Paste/Views/MainWindow/ClipboardCardView.swift (Wave 1 创建)
    - Z-Paste/ViewModels/ClipboardViewModel.swift (刚创建)
    - .planning/phases/02-main-interface/02-RESEARCH.md (键盘导航模式)
    - .planning/phases/02-main-interface/02-UI-SPEC.md (列表规格)
  </read_first>
  <action>
创建 CardListView.swift，实现横向滚动列表和键盘导航：

```swift
import SwiftUI

struct CardListView: View {
    @ObservedObject var viewModel: ClipboardViewModel
    @FocusState private var isFocused: Bool
    var onItemCopied: (() -> Void)?
    var onHide: (() -> Void)?

    var body: some View {
        Group {
            if viewModel.isEmpty {
                emptyStateView
            } else {
                cardScrollView
            }
        }
        .focused($isFocused)
        .onKeyPress(.leftArrow) {
            viewModel.selectPrevious()
            return .handled
        }
        .onKeyPress(.rightArrow) {
            viewModel.selectNext()
            return .handled
        }
        .onKeyPress(.return) {
            viewModel.copySelected()
            onItemCopied?()
            return .handled
        }
        .onKeyPress(.escape) {
            onHide?()
            return .handled
        }
        .onAppear {
            isFocused = true
        }
    }

    // MARK: - Card Scroll View
    private var cardScrollView: some View {
        ScrollViewReader { proxy in
            ScrollView(.horizontal, showsIndicators: false) {
                LazyHStack(spacing: 12) {
                    ForEach(Array(viewModel.items.enumerated()), id: \.element.id) { index, item in
                        ClipboardCardView(
                            item: item,
                            isSelected: index == viewModel.selectedIndex
                        ) {
                            viewModel.selectItem(at: index)
                        }
                        .id(item.id)
                    }
                }
                .padding(.horizontal, 16)
                .padding(.vertical, 12)
            }
            .onChange(of: viewModel.selectedIndex) { _, newIndex in
                if let id = viewModel.items[safe: newIndex]?.id {
                    withAnimation(.easeOut(duration: 0.15)) {
                        proxy.scrollTo(id, anchor: .center)
                    }
                }
            }
        }
    }

    // MARK: - Empty State
    private var emptyStateView: some View {
        VStack(spacing: 12) {
            Image(systemName: "doc.on.clipboard")
                .font(.system(size: 48))
                .foregroundColor(.secondary)

            Text("No Clipboard History")
                .font(.headline)

            Text("Copy something to see it appear here.\nPress Escape to close.")
                .font(.body)
                .foregroundColor(.secondary)
                .multilineTextAlignment(.center)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

// MARK: - Array Extension
private extension Array {
    subscript(safe index: Int) -> Element? {
        indices.contains(index) ? self[index] : nil
    }
}
```

**关键实现要点：**
1. 使用 `LazyHStack` 实现懒加载，提升性能
2. 卡片间距 12px（UI-SPEC.md）
3. 横向内边距 16px，纵向 12px
4. 键盘导航：左右箭头、Enter、Escape
5. 选中变化时自动滚动到可见区域
6. 空状态显示友好提示
</action>
  <verify>
    <automated>grep -q "struct CardListView" Z-Paste/Views/MainWindow/CardListView.swift && grep -q "LazyHStack" Z-Paste/Views/MainWindow/CardListView.swift && grep -q "onKeyPress" Z-Paste/Views/MainWindow/CardListView.swift</automated>
  </verify>
  <acceptance_criteria>
    - Z-Paste/Views/MainWindow/CardListView.swift 存在
    - 文件包含 `struct CardListView: View`
    - 文件包含 `@ObservedObject var viewModel`
    - 文件包含 `ScrollView(.horizontal, showsIndicators: false)`
    - 文件包含 `LazyHStack(spacing: 12)`
    - 文件包含 `.onKeyPress(.leftArrow)` 处理
    - 文件包含 `.onKeyPress(.rightArrow)` 处理
    - 文件包含 `.onKeyPress(.return)` 处理
    - 文件包含 `.onKeyPress(.escape)` 处理
    - 包含 emptyStateView 空状态视图
    - 使用 ScrollViewReader 实现自动滚动
  </acceptance_criteria>
  <done>CardListView 实现完成，支持横向滚动、键盘导航、自动滚动到选中项</done>
</task>

</tasks>

<verification>
**手动验证步骤：**
1. 运行应用，验证空状态显示
2. 添加测试数据，验证卡片列表显示
3. 按左右箭头键，验证选中状态切换
4. 验证选中卡片自动滚动到可见区域
5. 按 Escape 验证关闭回调触发
</verification>

<success_criteria>
- CardListView.swift 创建完成
- ClipboardViewModel.swift 创建完成
- 横向滚动使用 LazyHStack
- 键盘导航正确响应
- 空状态友好提示
- 选中项自动滚动
</success_criteria>

<output>
After completion, create `.planning/phases/02-main-interface/02-03-SUMMARY.md`
</output>
