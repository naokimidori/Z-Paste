---
phase: 02-main-interface
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - Z-Paste/Views/MainWindow/MainWindowView.swift
  - Z-Paste/App/AppDelegate.swift
  - Z-Paste/Views/ContentView.swift
autonomous: false
requirements: [2.1, 2.2, 2.3, 2.4, 2.5]
user_setup: []

must_haves:
  truths:
    - "按 Option + ` 快捷键唤起窗口"
    - "窗口从屏幕底部滑入显示"
    - "窗口显示横向滚动的卡片列表"
    - "点击窗口外部可关闭窗口"
    - "按 Escape 可关闭窗口"
    - "按 Enter 可选择并复制当前卡片"
  artifacts:
    - path: "Z-Paste/Views/MainWindow/MainWindowView.swift"
      provides: "主窗口视图容器"
      exports: ["MainWindowView"]
    - path: "Z-Paste/App/AppDelegate.swift"
      provides: "应用生命周期管理和窗口集成"
  key_links:
    - from: "AppDelegate"
      to: "WindowService"
      via: "WindowService.shared"
      pattern: "WindowService.shared"
    - from: "AppDelegate"
      to: "MainWindowView"
      via: "NSHostingController"
      pattern: "NSHostingController.*MainWindowView"
    - from: "MainWindowView"
      to: "CardListView"
      via: "直接引用"
      pattern: "CardListView(viewModel:"
---

<objective>
创建 MainWindowView 并集成到 AppDelegate

Purpose: 将所有组件组装成完整的主窗口，实现窗口行为和交互
Output: MainWindowView.swift + 修改后的 AppDelegate.swift
</objective>

<execution_context>
@/Users/longzhao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/longzhao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-main-interface/02-CONTEXT.md
@.planning/phases/02-main-interface/02-RESEARCH.md
@.planning/phases/02-main-interface/02-UI-SPEC.md
</context>

<interfaces>
<!-- Key types from Wave 1 and Wave 2 -->

From Z-Paste/Services/WindowService.swift:
```swift
class WindowService {
    static let shared: WindowService
    func createPanel(with contentView: NSView) -> NSPanel
    func showWindow()
    func hideWindow()
    func toggleWindow()
    var isVisible: Bool
}
```

From Z-Paste/ViewModels/ClipboardViewModel.swift:
```swift
@MainActor
class ClipboardViewModel: ObservableObject {
    @Published var items: [ClipboardItem]
    @Published var selectedIndex: Int
    func loadItems()
    func copySelected()
}
```

From Z-Paste/Views/MainWindow/CardListView.swift:
```swift
struct CardListView: View {
    @ObservedObject var viewModel: ClipboardViewModel
    var onItemCopied: (() -> Void)?
    var onHide: (() -> Void)?
}
```

From Z-Paste/Services/DatabaseService.swift:
```swift
class DatabaseService {
    func fetchRecent(limit: Int) throws -> [ClipboardItem]
}
```

From Z-Paste/Services/HotkeyService.swift:
```swift
class HotkeyService {
    var onToggleWindow: (() -> Void)?
    func register()
    func unregister()
}
```

From Z-Paste/App/AppDelegate.swift (current):
```swift
class AppDelegate: NSObject, NSApplicationDelegate {
    private let hotkeyService = HotkeyService()
    var mainWindow: NSWindow!
    private func toggleWindow()
}
```

From UI-SPEC.md - 窗口行为:
- Global hotkey pressed: Toggle show/hide with slide animation
- Window shown: Activate app, focus first card, load recent history
- Window hidden: Deactivate, clear selection state
- Click outside window: Hide window with slide animation
- Card selected (Enter): Copy content to clipboard, hide window
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 MainWindowView</name>
  <files>Z-Paste/Views/MainWindow/MainWindowView.swift</files>
  <read_first>
    - Z-Paste/Views/MainWindow/CardListView.swift (Wave 2 创建)
    - Z-Paste/ViewModels/ClipboardViewModel.swift (Wave 2 创建)
    - Z-Paste/Services/DatabaseService.swift (理解数据库依赖)
    - .planning/phases/02-main-interface/02-UI-SPEC.md (窗口规格)
  </read_first>
  <action>
创建 MainWindowView.swift，作为主窗口的内容视图：

```swift
import SwiftUI

struct MainWindowView: View {
    @StateObject private var viewModel: ClipboardViewModel
    var onHide: (() -> Void)?

    // 初始化器接收 DatabaseService 依赖
    init(database: DatabaseService, onHide: (() -> Void)? = nil) {
        self._viewModel = StateObject(wrappedValue: ClipboardViewModel(database: database))
        self.onHide = onHide
    }

    var body: some View {
        ZStack {
            // 背景 - 毛玻璃效果 (WindowService 已处理，这里可透明)
            Color.clear

            // 内容区域
            VStack(spacing: 0) {
                CardListView(
                    viewModel: viewModel,
                    onItemCopied: { [weak onHide] in
                        // 复制后关闭窗口
                        onHide?()
                    },
                    onHide: onHide
                )
            }
        }
        .frame(minWidth: 800, minHeight: 280)  // 最小窗口尺寸
        .onAppear {
            // 每次显示时重新加载数据
            viewModel.loadItems()
        }
    }
}

// MARK: - Preview
#Preview {
    // 预览使用模拟数据
    MainWindowView(database: try! DatabaseService(databasePath: ":memory:"))
        .frame(width: 1000, height: 280)
}
```

**关键实现要点：**
1. 使用 @StateObject 管理 ViewModel
2. onAppear 时重新加载数据，确保显示最新历史
3. onItemCopied 回调触发窗口关闭
4. 传递 onHide 回调给 CardListView
</action>
  <verify>
    <automated>grep -q "struct MainWindowView" Z-Paste/Views/MainWindow/MainWindowView.swift && grep -q "CardListView" Z-Paste/Views/MainWindow/MainWindowView.swift && grep -q "viewModel.loadItems" Z-Paste/Views/MainWindow/MainWindowView.swift</automated>
  </verify>
  <acceptance_criteria>
    - Z-Paste/Views/MainWindow/MainWindowView.swift 存在
    - 文件包含 `struct MainWindowView: View`
    - 文件包含 `@StateObject private var viewModel`
    - 文件引用 `CardListView`
    - 文件包含 `onAppear` 加载数据
    - 文件包含 `onHide` 回调
    - 文件包含 `onItemCopied` 回调
  </acceptance_criteria>
  <done>MainWindowView 创建完成，组合 CardListView 并提供窗口关闭回调</done>
</task>

<task type="auto">
  <name>Task 2: 重构 AppDelegate 集成所有组件</name>
  <files>
    Z-Paste/App/AppDelegate.swift
    Z-Paste/Views/ContentView.swift
  </files>
  <read_first>
    - Z-Paste/App/AppDelegate.swift (当前实现)
    - Z-Paste/Services/WindowService.swift (Wave 1 创建)
    - Z-Paste/Views/MainWindow/MainWindowView.swift (刚创建)
    - Z-Paste/Services/DatabaseService.swift (数据库服务)
    - Z-Paste/Services/ClipboardService.swift (剪贴板服务)
    - Z-Paste/Services/HotkeyService.swift (快捷键服务)
  </read_first>
  <action>
重构 AppDelegate.swift，集成所有服务：

```swift
import AppKit
import SwiftUI

class AppDelegate: NSObject, NSApplicationDelegate {
    // MARK: - Services
    private let hotkeyService = HotkeyService()
    private let clipboardService = ClipboardService()
    private var databaseService: DatabaseService!
    private let windowService = WindowService.shared

    // MARK: - State
    private var panel: NSPanel?

    // MARK: - Lifecycle
    func applicationDidFinishLaunching(_ notification: Notification) {
        // 1. 初始化数据库
        do {
            let dbPath = databasePath()
            databaseService = try DatabaseService(databasePath: dbPath)
        } catch {
            print("Failed to initialize database: \(error)")
            return
        }

        // 2. 配置应用
        configureApp()

        // 3. 创建主窗口
        createMainWindow()

        // 4. 注册快捷键
        hotkeyService.register()
        hotkeyService.onToggleWindow = { [weak self] in
            self?.toggleWindow()
        }

        // 5. 启动剪贴板监听
        clipboardService.onNewItem = { [weak self] _ in
            // 新项目添加时，如果窗口可见则刷新
            if self?.windowService.isVisible == true {
                // ViewModel 会在 onAppear 时自动加载
            }
        }
        clipboardService.startMonitoring()

        print("Z-Paste 应用已启动")
    }

    func applicationWillTerminate(_ notification: Notification) {
        hotkeyService.unregister()
        clipboardService.stopMonitoring()
        print("Z-Paste 应用正在退出")
    }

    func applicationShouldTerminateAfterLastWindowClosed(_ sender: NSApplication) -> Bool {
        return false // 窗口关闭时不退出应用
    }

    // MARK: - Private Methods

    private func configureApp() {
        // 隐藏 Dock 图标
        NSApp.setActivationPolicy(.accessory)
    }

    private func createMainWindow() {
        // 创建 MainWindowView
        let mainWindowView = MainWindowView(database: databaseService) { [weak self] in
            self?.hideWindow()
        }

        // 创建 NSHostingController
        let hostingController = NSHostingController(rootView: mainWindowView)

        // 创建 Panel
        panel = windowService.createPanel(with: hostingController.view)

        // 设置点击外部关闭
        setupClickOutsideHandling()
    }

    private func toggleWindow() {
        windowService.toggleWindow()
    }

    private func hideWindow() {
        windowService.hideWindow()
    }

    private func showWindow() {
        windowService.showWindow()
    }

    // MARK: - Click Outside Handling
    private func setupClickOutsideHandling() {
        // 监听应用失去激活事件
        NotificationCenter.default.addObserver(
            self,
            selector: #selector(applicationDidResignActive),
            name: NSApplication.didResignActiveNotification,
            object: nil
        )
    }

    @objc private func applicationDidResignActive(_ notification: Notification) {
        // 窗口失去焦点时关闭
        if windowService.isVisible {
            hideWindow()
        }
    }

    // MARK: - Helpers
    private func databasePath() -> String {
        let fileManager = FileManager.default
        let appSupport = fileManager.urls(for: .applicationSupportDirectory, in: .userDomainMask).first!
        let appDir = appSupport.appendingPathComponent("Z-Paste")

        // 确保目录存在
        try? fileManager.createDirectory(at: appDir, withIntermediateDirectories: true)

        return appDir.appendingPathComponent("clipboard.sqlite").path
    }
}
```

**更新 ContentView.swift** (简化为入口提示):
```swift
import SwiftUI

/// 入口视图 - 不显示任何内容，实际窗口由 AppDelegate 管理
struct ContentView: View {
    var body: some View {
        EmptyView()
    }
}
```

**关键集成点：**
1. 初始化 DatabaseService 并传递给 MainWindowView
2. 使用 WindowService 替代直接创建 NSWindow
3. 快捷键触发 toggleWindow
4. 监听应用失去激活事件实现点击外部关闭
5. 启动 ClipboardService 监听剪贴板变化
</action>
  <verify>
    <automated>grep -q "WindowService.shared" Z-Paste/App/AppDelegate.swift && grep -q "MainWindowView(database:" Z-Paste/App/AppDelegate.swift && grep -q "didResignActiveNotification" Z-Paste/App/AppDelegate.swift</automated>
  </verify>
  <acceptance_criteria>
    - AppDelegate.swift 包含 `WindowService.shared` 引用
    - AppDelegate.swift 包含 `DatabaseService` 初始化
    - AppDelegate.swift 包含 `MainWindowView(database:` 创建
    - AppDelegate.swift 包含 `didResignActiveNotification` 监听
    - AppDelegate.swift 包含 `clipboardService.startMonitoring()`
    - ContentView.swift 简化为 EmptyView
  </acceptance_criteria>
  <done>AppDelegate 重构完成，集成所有服务和窗口管理</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
完整的底部弹出窗口功能：
- WindowService 提供 NSPanel 和滑入/滑出动画
- ClipboardCardView 卡片组件
- CardListView 横向滚动列表
- MainWindowView 主窗口视图
- AppDelegate 集成所有组件
  </what-built>
  <how-to-verify>
1. 运行应用 (Cmd+R 在 Xcode 或 `swift run` 命令行)
2. 按 Option + ` 快捷键唤起窗口
3. 验证窗口从屏幕底部滑入，动画流畅
4. 验证窗口位置在屏幕底部中央
5. 如果有剪贴板历史，验证卡片显示正确
6. 按左右箭头键验证卡片选择
7. 按 Escape 验证窗口关闭
8. 点击窗口外部验证窗口关闭
9. 按 Option + ` 再次打开，验证历史数据加载
  </how-to-verify>
  <resume-signal>输入 "approved" 继续或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
**验证清单：**
- [ ] 应用启动无错误
- [ ] Option + ` 唤起窗口
- [ ] 窗口从底部滑入动画
- [ ] 卡片横向滚动
- [ ] 键盘导航正常
- [ ] 点击外部关闭窗口
- [ ] ESC 关闭窗口
- [ ] Enter 选择并复制 (Phase 3 完整实现)
</verification>

<success_criteria>
- 所有组件集成正确
- 窗口显示/隐藏动画流畅
- 键盘导航响应正确
- 点击外部关闭正常
- 数据加载正确
</success_criteria>

<output>
After completion, create `.planning/phases/02-main-interface/02-04-SUMMARY.md`
</output>
