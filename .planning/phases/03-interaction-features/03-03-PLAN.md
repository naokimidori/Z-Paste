---
phase: 03-interaction-features
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - Tests/Z-PasteTests/ClipboardViewModelBatchTests.swift
  - Z-Paste/ViewModels/ClipboardViewModel.swift
  - Z-Paste/Views/MainWindow/ClipboardCardView.swift
  - Z-Paste/Views/MainWindow/CardListView.swift
  - Z-Paste/Views/MainWindow/MainWindowView.swift
autonomous: true
requirements:
  - ROADMAP-3.5
must_haves:
  truths:
    - "批量能力以轻量入口出现，不会抢占单击即粘贴的普通流程"
    - "进入批量模式后，用户可以多选多项并执行批量收藏或批量删除"
    - "退出批量模式后，界面回到普通单选与主动作模式"
    - "批量收藏/删除完成后，当前窗口会立即反映结果"
  artifacts:
    - path: "Tests/Z-PasteTests/ClipboardViewModelBatchTests.swift"
      provides: "批量模式状态与批量管理动作测试"
    - path: "Z-Paste/ViewModels/ClipboardViewModel.swift"
      provides: "isMultiSelectMode / selectedItemIDs / batch actions"
      exports: ["toggleMultiSelectMode()", "toggleBatchSelection(for:)", "favoriteSelectedItems()", "deleteSelectedItems()"]
    - path: "Z-Paste/Views/MainWindow/MainWindowView.swift"
      provides: "批量模式轻量入口与动作按钮"
    - path: "Z-Paste/Views/MainWindow/ClipboardCardView.swift"
      provides: "多选态视觉反馈"
  key_links:
    - from: "Z-Paste/Views/MainWindow/MainWindowView.swift"
      to: "Z-Paste/ViewModels/ClipboardViewModel.swift"
      via: "lightweight batch mode toggle"
      pattern: "toggleMultiSelectMode\(|favoriteSelectedItems\(|deleteSelectedItems\("
    - from: "Z-Paste/Views/MainWindow/CardListView.swift"
      to: "selectedItemIDs"
      via: "tap switches to batch selection only when isMultiSelectMode is true"
      pattern: "isMultiSelectMode|toggleBatchSelection"
    - from: "Z-Paste/ViewModels/ClipboardViewModel.swift"
      to: "existing single-item delete/favorite logic"
      via: "reuse deleteItem(_:) and toggleFavorite(for:) or same database operations"
      pattern: "deleteItem\(|toggleFavorite\(for:|selectedItemIDs"
---

<objective>
以轻量方式补齐 Phase 3 的低优先级批量操作，只支持批量收藏与批量删除，并明确保持普通单击即粘贴主流程不变。

Purpose: 满足历史管理扩展能力，同时把复杂度控制在当前阶段边界内。
Output: 批量模式测试、ViewModel 多选状态、轻量批量入口与多选视觉反馈。
</objective>

<execution_context>
@/Users/longzhao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/longzhao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@/Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-CONTEXT.md
@/Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-RESEARCH.md
@/Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-VALIDATION.md
@.planning/phases/03-interaction-features/03-01-PLAN.md
@.planning/phases/03-interaction-features/03-02-PLAN.md
</context>

<interfaces>
From Z-Paste/ViewModels/ClipboardViewModel.swift (after 03-02):
```swift
@MainActor
class ClipboardViewModel: ObservableObject {
    @Published var items: [ClipboardItem]
    @Published var selectedIndex: Int
    func copySelected()
    func copyItemOnly(_ item: ClipboardItem)
    func toggleFavorite(for item: ClipboardItem)
    func deleteItem(_ item: ClipboardItem)
}
```

From Z-Paste/Views/MainWindow/CardListView.swift:
```swift
struct CardListView: View {
    @ObservedObject var viewModel: ClipboardViewModel
    var onHide: (() -> Void)?
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 用测试固定轻量批量模式边界与状态机</name>
  <files>
    Tests/Z-PasteTests/ClipboardViewModelBatchTests.swift
    Z-Paste/ViewModels/ClipboardViewModel.swift
  </files>
  <read_first>
    - Z-Paste/ViewModels/ClipboardViewModel.swift
    - /Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-CONTEXT.md
    - /Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: 开启批量模式后，点击卡片只切换多选状态，不执行主动作。
    - Test 2: 退出批量模式会清空 `selectedItemIDs` 并恢复普通模式。
    - Test 3: `favoriteSelectedItems()` 只批量收藏当前选中集合。
    - Test 4: `deleteSelectedItems()` 删除后更新 `items`、清空选中集合，并保留合法 `selectedIndex`。
  </behavior>
  <action>
    新建 `Tests/Z-PasteTests/ClipboardViewModelBatchTests.swift`，并在 `ClipboardViewModel` 中先定义与测试匹配的批量状态接口，使用以下明确命名：

    - `@Published var isMultiSelectMode: Bool = false`
    - `@Published var selectedItemIDs: Set<Int64> = []`
    - `func toggleMultiSelectMode()`
    - `func toggleBatchSelection(for item: ClipboardItem)`
    - `func favoriteSelectedItems()`
    - `func deleteSelectedItems()`

    测试函数名至少包括：
    - `testToggleMultiSelectModeClearsSelectionWhenTurningOff()`
    - `testToggleBatchSelectionAddsAndRemovesIDs()`
    - `testFavoriteSelectedItemsUpdatesOnlySelectedItems()`
    - `testDeleteSelectedItemsRemovesItemsAndKeepsValidSelection()`

    严格遵守边界：不要在批量模式里实现批量粘贴、复杂排序、工具栏体系、搜索后批量，且不要让普通模式点击语义改变。
  </action>
  <verify>
    <automated>xcodebuild test -project "/Users/longzhao/aicodes/Z-Paste/Z-Paste.xcodeproj" -scheme "Z-Paste" -destination 'platform=macOS' -only-testing:Z-PasteTests/ClipboardViewModelBatchTests</automated>
  </verify>
  <acceptance_criteria>
    - `Tests/Z-PasteTests/ClipboardViewModelBatchTests.swift` 包含字符串 `final class ClipboardViewModelBatchTests: XCTestCase`
    - `Tests/Z-PasteTests/ClipboardViewModelBatchTests.swift` 包含字符串 `func testToggleMultiSelectModeClearsSelectionWhenTurningOff()`
    - `Tests/Z-PasteTests/ClipboardViewModelBatchTests.swift` 包含字符串 `func testDeleteSelectedItemsRemovesItemsAndKeepsValidSelection()`
    - `Z-Paste/ViewModels/ClipboardViewModel.swift` 包含字符串 `@Published var isMultiSelectMode: Bool = false`
    - `Z-Paste/ViewModels/ClipboardViewModel.swift` 包含字符串 `@Published var selectedItemIDs: Set<Int64> = []`
    - `Z-Paste/ViewModels/ClipboardViewModel.swift` 包含字符串 `func favoriteSelectedItems()`
    - `Z-Paste/ViewModels/ClipboardViewModel.swift` 包含字符串 `func deleteSelectedItems()`
  </acceptance_criteria>
  <done>批量模式边界与状态机已通过测试固定，后续 UI 接入不会把普通点击流程意外改坏。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在界面加入低干扰批量入口与多选视觉态</name>
  <files>
    Z-Paste/ViewModels/ClipboardViewModel.swift
    Z-Paste/Views/MainWindow/ClipboardCardView.swift
    Z-Paste/Views/MainWindow/CardListView.swift
    Z-Paste/Views/MainWindow/MainWindowView.swift
  </files>
  <read_first>
    - Z-Paste/ViewModels/ClipboardViewModel.swift
    - Z-Paste/Views/MainWindow/ClipboardCardView.swift
    - Z-Paste/Views/MainWindow/CardListView.swift
    - Z-Paste/Views/MainWindow/MainWindowView.swift
    - /Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-CONTEXT.md
    - /Users/longzhao/aicodes/Z-Paste/.planning/phases/02-main-interface/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: 普通模式下卡片单击仍然执行主动作。
    - Test 2: 批量模式下卡片单击只切换多选态。
    - Test 3: 界面仅提供批量收藏与批量删除两个管理动作。
  </behavior>
  <action>
    使用最小 UI 增量把批量模式接进现有 MainWindow，不新增复杂容器：

    1. 在 `MainWindowView` 顶部或列表上方加入一个轻量切换入口，使用明确文案之一：
       - 进入时 `Button("批量操作")`
       - 批量模式中 `Button("完成")`
       与之配套显示两个动作按钮：`Button("批量收藏")`、`Button("批量删除")`。
    2. 在 `CardListView` 中把卡片点击逻辑改为显式分支：
       - `if viewModel.isMultiSelectMode { viewModel.toggleBatchSelection(for: item) }`
       - `else { runPrimaryActionForCurrentSelection() }`
       保证普通模式下仍维持 03-01 已建立的单击即主动作。
    3. 在 `ClipboardCardView` 中增加批量模式视觉参数，使用明确属性名：
       - `let isMultiSelectMode: Bool`
       - `let isBatchSelected: Bool`
       用现有边框/角标/透明覆盖层之一表达多选态，但保持 250x250 卡片尺寸、星标位置、三段式布局不变。
    4. 批量模式操作只调用 `favoriteSelectedItems()` 与 `deleteSelectedItems()`；不要把入口塞进右键菜单，不要新增全屏编辑模式，不要改变右键菜单结构。
    5. 退出批量模式后立即清空多选态，让普通模式重新恢复单选高亮和单击主动作语义。
  </action>
  <verify>
    <automated>xcodebuild test -project "/Users/longzhao/aicodes/Z-Paste/Z-Paste.xcodeproj" -scheme "Z-Paste" -destination 'platform=macOS' -only-testing:Z-PasteTests/ClipboardViewModelBatchTests -only-testing:Z-PasteTests/ClipboardViewModelInteractionTests</automated>
  </verify>
  <acceptance_criteria>
    - `Z-Paste/Views/MainWindow/MainWindowView.swift` 包含字符串 `Button("批量操作")` 或 `Button("完成")`
    - `Z-Paste/Views/MainWindow/MainWindowView.swift` 包含字符串 `Button("批量收藏")`
    - `Z-Paste/Views/MainWindow/MainWindowView.swift` 包含字符串 `Button("批量删除")`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 包含字符串 `if viewModel.isMultiSelectMode`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 包含字符串 `toggleBatchSelection(for:`
    - `Z-Paste/Views/MainWindow/ClipboardCardView.swift` 包含字符串 `let isMultiSelectMode: Bool`
    - `Z-Paste/Views/MainWindow/ClipboardCardView.swift` 包含字符串 `let isBatchSelected: Bool`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 仍包含字符串 `runPrimaryActionForCurrentSelection()`
  </acceptance_criteria>
  <done>批量入口已接入且足够轻量，批量模式与普通模式边界清晰，普通单击即主动作流程保持不变。</done>
</task>

</tasks>

<verification>
- 运行 `xcodebuild test -project "/Users/longzhao/aicodes/Z-Paste/Z-Paste.xcodeproj" -scheme "Z-Paste" -destination 'platform=macOS' -only-testing:Z-PasteTests/ClipboardViewModelBatchTests -only-testing:Z-PasteTests/ClipboardViewModelInteractionTests` 通过。
- 手工验证：普通模式单击仍直接执行主动作；进入批量模式后单击仅切换多选；批量收藏/删除结果即时可见；退出后恢复普通语义。
</verification>

<success_criteria>
- 批量能力只覆盖批量收藏与批量删除。
- 批量模式为显式进入/退出，不抢占单击即粘贴主流程。
- 多选视觉态清晰，但不破坏 Phase 2 锁定的卡片布局与尺寸。
- 批量操作完成后当前窗口即时反馈，退出后恢复普通单选模式。
</success_criteria>

<output>
After completion, create `.planning/phases/03-interaction-features/03-03-SUMMARY.md`
</output>
