---
phase: 03-interaction-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Tests/Z-PasteTests/ClipboardServiceWritebackTests.swift
  - Tests/Z-PasteTests/ClipboardViewModelInteractionTests.swift
  - Z-Paste/Services/ClipboardService.swift
  - Z-Paste/ViewModels/ClipboardViewModel.swift
  - Z-Paste/Views/MainWindow/CardListView.swift
  - Z-Paste/Views/MainWindow/MainWindowView.swift
  - Z-Paste/App/AppDelegate.swift
  - Z-Paste/Services/WindowService.swift
autonomous: true
requirements:
  - FR-3.5
  - ROADMAP-3.1
must_haves:
  truths:
    - "用户单击卡片与按 Enter 会触发同一条主动作链路"
    - "主动作会先把历史项写回系统剪贴板，再尝试向前台应用发送粘贴"
    - "面板会在主动作完成后关闭；缺少 Accessibility 权限时仍会保留仅复制降级结果"
    - "文本、富文本、图片、文件四种已支持类型都能进入同一主动作编排"
  artifacts:
    - path: "Z-Paste/Services/ClipboardService.swift"
      provides: "writeBack + paste attempt + self-write suppression"
      exports: ["writeItemToPasteboard(_:)", "attemptPasteAfterWindowHide()", "performPrimaryAction(for:)"]
    - path: "Z-Paste/ViewModels/ClipboardViewModel.swift"
      provides: "统一主动作入口与关闭回调"
      exports: ["copySelected()", "performPrimaryAction(for:)"]
    - path: "Tests/Z-PasteTests/ClipboardServiceWritebackTests.swift"
      provides: "写回与降级路径自动化验证"
    - path: "Tests/Z-PasteTests/ClipboardViewModelInteractionTests.swift"
      provides: "点击/Enter 统一动作自动化验证"
  key_links:
    - from: "Z-Paste/Views/MainWindow/CardListView.swift"
      to: "Z-Paste/ViewModels/ClipboardViewModel.swift"
      via: "tap gesture and Enter key both call copySelected()/performPrimaryAction(for:)"
      pattern: "copySelected\(|performPrimaryAction\(for:"
    - from: "Z-Paste/ViewModels/ClipboardViewModel.swift"
      to: "Z-Paste/Services/ClipboardService.swift"
      via: "primary action orchestration"
      pattern: "clipboardService\.performPrimaryAction|writeItemToPasteboard"
    - from: "Z-Paste/App/AppDelegate.swift"
      to: "Z-Paste/Services/WindowService.swift"
      via: "hide panel before paste attempt or hand-off callback"
      pattern: "hideWindow\(|attemptPasteAfterWindowHide|performPrimaryAction"
---

<objective>
建立 Phase 3 的主动作闭环：统一点击与 Enter，写回剪贴板，尝试粘贴到当前应用，并在窗口关闭后完成降级友好的收尾。

Purpose: 让 Z-Paste 从“展示历史”进入“可直接使用历史项”的核心价值链路。
Output: 主动作自动化测试、ClipboardService 写回/粘贴能力、ViewModel/视图/AppDelegate 的统一接线。
</objective>

<execution_context>
@/Users/longzhao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/longzhao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@/Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-CONTEXT.md
@/Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-RESEARCH.md
@/Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-VALIDATION.md
@/Users/longzhao/aicodes/Z-Paste/.planning/phases/02-main-interface/02-UI-SPEC.md
@.planning/phases/02-main-interface/02-03-SUMMARY.md
@.planning/phases/02-main-interface/02-04-SUMMARY.md
</context>

<interfaces>
From Z-Paste/Models/ClipboardItem.swift:
```swift
enum ItemType: String, Codable, CaseIterable {
    case text
    case image
    case file
    case rtf
}

struct ClipboardItem: Codable, Identifiable, Equatable, Hashable {
    var id: Int64?
    var content: String
    var itemType: ItemType
    var sourceApp: String?
    var sourceAppIcon: Data?
    var createdAt: Date
    var isFavorite: Bool
    var data: Data?
    var contentHash: String { get }
}
```

From Z-Paste/ViewModels/ClipboardViewModel.swift:
```swift
@MainActor
class ClipboardViewModel: ObservableObject {
    @Published var items: [ClipboardItem]
    @Published var selectedIndex: Int
    var selectedItem: ClipboardItem? { get }
    func selectItem(at index: Int)
    func copySelected()
}
```

From Z-Paste/Views/MainWindow/CardListView.swift:
```swift
struct CardListView: View {
    @ObservedObject var viewModel: ClipboardViewModel
    var onItemCopied: (() -> Void)?
    var onHide: (() -> Void)?
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为主动作链路建立测试基线与依赖边界</name>
  <files>
    Tests/Z-PasteTests/ClipboardServiceWritebackTests.swift
    Tests/Z-PasteTests/ClipboardViewModelInteractionTests.swift
    Z-Paste/Services/ClipboardService.swift
    Z-Paste/ViewModels/ClipboardViewModel.swift
  </files>
  <read_first>
    - Tests/Z-PasteTests/ClipboardServiceTests.swift
    - Z-Paste/Services/ClipboardService.swift
    - Z-Paste/ViewModels/ClipboardViewModel.swift
    - Z-Paste/Models/ClipboardItem.swift
    - /Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-CONTEXT.md
    - /Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `copySelected()` 对文本项调用主动作服务，并产生“需要关闭窗口 + 已写回剪贴板”的结果。
    - Test 2: 缺少 Accessibility 权限时，主动作结果为 `copiedOnly`，而不是失败中断。
    - Test 3: `writeItemToPasteboard(_:)` 支持 `.text`、`.rtf`、`.image`、`.file` 四种类型分支。
    - Test 4: 程序化写回后下一次监听轮询会被忽略一次，不会生成重复历史。
  </behavior>
  <action>
    新建并接通 Phase 3 的主动作测试基线，先让测试能描述目标行为，再让生产代码具备可测边界：

    1. 创建 `Tests/Z-PasteTests/ClipboardServiceWritebackTests.swift`，至少包含以下测试函数名：
       - `testWriteItemToPasteboardWritesTextAndSkipsNextMonitorSave()`
       - `testWriteItemToPasteboardWritesRTFAndPlainTextFallback()`
       - `testWriteItemToPasteboardWritesImageData()`
       - `testWriteItemToPasteboardWritesFileURLsFromNewlineSeparatedPaths()`
       - `testPerformPrimaryActionReturnsCopiedOnlyWhenAccessibilityIsUnavailable()`
    2. 创建 `Tests/Z-PasteTests/ClipboardViewModelInteractionTests.swift`，至少包含以下测试函数名：
       - `testCopySelectedRequestsPrimaryActionForSelectedItem()`
       - `testCopySelectedDoesNothingWhenSelectionIsInvalid()`
    3. 在 `ClipboardService` 中引入可注入边界，使用具体协议名：
       - `PasteboardWriting`
       - `AccessibilityTrustChecking`
       - `PasteEventSending`
       这些协议仅用于隔离 `NSPasteboard.general`、`AXIsProcessTrusted()`、`CGEvent` 发送，禁止新增第三方库。
    4. 在 `ClipboardViewModel` 中加入可测试的主动作结果回调或状态出口，使用一个明确枚举名，例如 `PrimaryActionResult`，至少包含 `pasted`、`copiedOnly`、`failed` 三种情况。
    5. 若现有 `Tests/Z-PasteTests/ClipboardServiceTests.swift` 与真实接口冲突，不要在新计划里继续复用旧签名；保留旧文件由执行者按最小改动修正或停用，但新测试必须成为可信基线。
  </action>
  <verify>
    <automated>xcodebuild test -project "/Users/longzhao/aicodes/Z-Paste/Z-Paste.xcodeproj" -scheme "Z-Paste" -destination 'platform=macOS' -only-testing:Z-PasteTests/ClipboardServiceWritebackTests -only-testing:Z-PasteTests/ClipboardViewModelInteractionTests</automated>
  </verify>
  <acceptance_criteria>
    - `Tests/Z-PasteTests/ClipboardServiceWritebackTests.swift` 包含字符串 `final class ClipboardServiceWritebackTests: XCTestCase`
    - `Tests/Z-PasteTests/ClipboardServiceWritebackTests.swift` 包含字符串 `func testPerformPrimaryActionReturnsCopiedOnlyWhenAccessibilityIsUnavailable()`
    - `Tests/Z-PasteTests/ClipboardViewModelInteractionTests.swift` 包含字符串 `func testCopySelectedRequestsPrimaryActionForSelectedItem()`
    - `Z-Paste/Services/ClipboardService.swift` 包含字符串 `protocol PasteboardWriting`
    - `Z-Paste/Services/ClipboardService.swift` 包含字符串 `protocol AccessibilityTrustChecking`
    - `Z-Paste/Services/ClipboardService.swift` 包含字符串 `protocol PasteEventSending`
    - `Z-Paste/ViewModels/ClipboardViewModel.swift` 包含字符串 `enum PrimaryActionResult`
  </acceptance_criteria>
  <done>主动作链路已有可执行测试和可注入边界，后续实现不需要再探索如何测试写回、权限和粘贴结果。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现统一点击/Enter 主动作与窗口关闭协调</name>
  <files>
    Z-Paste/Services/ClipboardService.swift
    Z-Paste/ViewModels/ClipboardViewModel.swift
    Z-Paste/Views/MainWindow/CardListView.swift
    Z-Paste/Views/MainWindow/MainWindowView.swift
    Z-Paste/App/AppDelegate.swift
    Z-Paste/Services/WindowService.swift
  </files>
  <read_first>
    - Z-Paste/Services/ClipboardService.swift
    - Z-Paste/ViewModels/ClipboardViewModel.swift
    - Z-Paste/Views/MainWindow/CardListView.swift
    - Z-Paste/Views/MainWindow/MainWindowView.swift
    - Z-Paste/App/AppDelegate.swift
    - Z-Paste/Services/WindowService.swift
    - /Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-CONTEXT.md
    - /Users/longzhao/aicodes/Z-Paste/.planning/phases/03-interaction-features/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: 点击卡片与按 Enter 走同一方法，不存在一条只是选中、另一条才执行主动作。
    - Test 2: 主动作顺序为“写回剪贴板 -> 请求关闭窗口 -> 尝试粘贴或降级”。
    - Test 3: 自写回抑制逻辑会跳过下一次轮询入库。
  </behavior>
  <action>
    按锁定决策把主动作落地到现有 SwiftUI/AppKit 结构中，使用以下具体状态与方法名进行收敛：

    1. 在 `ClipboardService` 中实现：
       - `func writeItemToPasteboard(_ item: ClipboardItem) throws`
       - `func attemptPasteAfterWindowHide() -> PrimaryActionResult`
       - `func performPrimaryAction(for item: ClipboardItem) -> PrimaryActionResult`
       其中 `performPrimaryAction(for:)` 的顺序固定为：写回 -> 标记忽略下一次监听 -> 返回可供上层决定是否隐藏的结果；不要把窗口逻辑塞进服务层。
    2. 在 `ClipboardViewModel` 中保留 `copySelected()` 作为稳定入口，但内部改为：
       - 读取 `selectedItem`
       - 调用 `performPrimaryAction(for:)`
       - 通过明确闭包名 `onPrimaryActionCompleted` 把 `PrimaryActionResult` 上抛给视图层
    3. 在 `CardListView` 中让以下两个入口调用完全相同的方法：
       - `KeyEventHandlingView.onReturn`
       - `ClipboardCardView.onTap`
       使用一个明确私有方法名，例如 `runPrimaryActionForCurrentSelection()`；禁止 tap 只做 `selectItem(at:)` 而不执行主动作。
    4. 在 `MainWindowView` 和 `AppDelegate` 中把 Phase 2 的 `onItemCopied` 语义统一替换为 `onPrimaryActionCompleted`；窗口关闭逻辑固定为“先 hideWindow，再由完成回调驱动粘贴尝试或 copied-only 提示态”。
    5. 在 `WindowService` 中只做窗口显示/隐藏，不新增收藏、删除或菜单状态；若需要延迟粘贴，延迟值写死为一个明确常量，例如 `0.08` 秒，并在代码中留下注释说明它用于等待焦点回流。
    6. 缺少 Accessibility 权限时，只返回 `PrimaryActionResult.copiedOnly`；不要请求网络权限、不要引入脚本桥接、不要改变锁定的“执行后自动关闭窗口”。
  </action>
  <verify>
    <automated>xcodebuild test -project "/Users/longzhao/aicodes/Z-Paste/Z-Paste.xcodeproj" -scheme "Z-Paste" -destination 'platform=macOS' -only-testing:Z-PasteTests/ClipboardServiceWritebackTests -only-testing:Z-PasteTests/ClipboardViewModelInteractionTests</automated>
  </verify>
  <acceptance_criteria>
    - `Z-Paste/Services/ClipboardService.swift` 包含字符串 `func writeItemToPasteboard(_ item: ClipboardItem) throws`
    - `Z-Paste/Services/ClipboardService.swift` 包含字符串 `func attemptPasteAfterWindowHide() -> PrimaryActionResult`
    - `Z-Paste/Services/ClipboardService.swift` 包含字符串 `func performPrimaryAction(for item: ClipboardItem) -> PrimaryActionResult`
    - `Z-Paste/ViewModels/ClipboardViewModel.swift` 包含字符串 `var onPrimaryActionCompleted: ((PrimaryActionResult) -> Void)?`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 同时包含字符串 `runPrimaryActionForCurrentSelection()` 和 `onReturn:`
    - `Z-Paste/Views/MainWindow/CardListView.swift` 同时包含字符串 `copySelected()` 与 `onTap:`
    - `Z-Paste/App/AppDelegate.swift` 包含字符串 `onPrimaryActionCompleted`
    - `Z-Paste/Services/ClipboardService.swift` 包含字符串 `copiedOnly`
  </acceptance_criteria>
  <done>点击与 Enter 已统一到同一主动作链路，窗口关闭与粘贴降级顺序明确，且自写回不会污染历史。</done>
</task>

</tasks>

<verification>
- 运行 `xcodebuild test -project "/Users/longzhao/aicodes/Z-Paste/Z-Paste.xcodeproj" -scheme "Z-Paste" -destination 'platform=macOS' -only-testing:Z-PasteTests/ClipboardServiceWritebackTests -only-testing:Z-PasteTests/ClipboardViewModelInteractionTests` 通过。
- 手工验证文本项：单击与 Enter 都会关闭窗口，并在有权限时尝试粘贴；无权限时至少完成复制。
- 重新打开窗口确认不会因主动写回生成重复历史。
</verification>

<success_criteria>
- 点击卡片与 Enter 的实现入口合并为一条主链路。
- 主动作支持 text / rtf / image / file 四类历史。
- 自动粘贴失败时存在 copied-only 降级，不阻塞窗口关闭。
- 主动写回后不会立刻再次被监听器保存成新记录。
</success_criteria>

<output>
After completion, create `.planning/phases/03-interaction-features/03-01-SUMMARY.md`
</output>
