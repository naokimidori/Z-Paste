---
phase: 01-project-foundation
plan: 06
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Z-Paste/App/Z_PasteApp.swift
  - Z-Paste/App/main.swift
  - Z-Paste.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - "1.1: 创建 Xcode 项目和基础目录结构"
gap_closure: true
must_haves:
  truths:
    - "应用使用 SwiftUI @main 入口并桥接 AppDelegate"
    - "Xcode 项目能成功编译"
  artifacts:
    - path: "Z-Paste/App/Z_PasteApp.swift"
      provides: "SwiftUI 应用入口"
      contains: "@main"
    - path: "Z-Paste.xcodeproj/project.pbxproj"
      provides: "目标源码列表包含 Z_PasteApp.swift"
      contains: "Z_PasteApp.swift"
  key_links:
    - from: "Z-Paste/App/Z_PasteApp.swift"
      to: "Z-Paste/App/AppDelegate.swift"
      via: "@NSApplicationDelegateAdaptor"
      pattern: "@NSApplicationDelegateAdaptor\\s*\\(AppDelegate\\.self\\)"
---

<objective>
恢复 SwiftUI @main 入口并桥接 AppDelegate

Purpose: 修复 Phase 01 回归缺口，确保应用入口与计划一致且可编译
Output: Z_PasteApp.swift 入口文件与工程配置恢复
</objective>

<execution_context>
@/Users/longzhao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/longzhao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-VERIFICATION.md
@Z-Paste/App/AppDelegate.swift
@Z-Paste/App/main.swift
</context>

<interfaces>
From Z-Paste/App/AppDelegate.swift:
```swift
class AppDelegate: NSObject, NSApplicationDelegate {
    func applicationDidFinishLaunching(_ notification: Notification)
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 恢复 SwiftUI 应用入口并移除 main.swift 启动</name>
  <files>Z-Paste/App/Z_PasteApp.swift, Z-Paste/App/main.swift, Z-Paste.xcodeproj/project.pbxproj</files>
  <read_first>
    - Z-Paste/App/AppDelegate.swift
    - Z-Paste/App/main.swift
    - .planning/phases/01-project-foundation/01-CONTEXT.md
    - .planning/phases/01-project-foundation/01-VERIFICATION.md
  </read_first>
  <action>
    新建 Z-Paste/App/Z_PasteApp.swift，使用 SwiftUI @main 入口并桥接 AppDelegate：
    - 声明 `@main` 的 App 结构体（命名为 Z_PasteApp）
    - 添加 `@NSApplicationDelegateAdaptor(AppDelegate.self) var appDelegate`
    - body 返回 `WindowGroup { EmptyView() }` 或与现有窗口托管逻辑一致的最小 SwiftUI 容器

    处理现有 main.swift 启动方式：
    - 从工程目标中移除 Z-Paste/App/main.swift 的编译源
    - 保留文件时将其改为注释说明（不包含 `NSApplication.shared` 或 `app.run()`），或直接删除该文件

    更新 Z-Paste.xcodeproj/project.pbxproj：
    - 确保 Z_PasteApp.swift 被加入目标 Sources
    - 确保 main.swift 不在 Sources 列表中，避免双入口冲突
  </action>
  <acceptance_criteria>
    - `Z-Paste/App/Z_PasteApp.swift` 存在且包含 `@main` 声明
    - `Z-Paste/App/Z_PasteApp.swift` 包含 `@NSApplicationDelegateAdaptor(AppDelegate.self)`
    - `Z-Paste.xcodeproj/project.pbxproj` 包含 `Z_PasteApp.swift` 且不包含 `main.swift` 的 Sources 引用
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Z-Paste.xcodeproj -scheme Z-Paste -destination 'platform=macOS' build 2>&1 | grep -q "BUILD SUCCEEDED" && echo "OK: build succeeded"</automated>
  </verify>
  <done>SwiftUI @main 入口恢复并桥接 AppDelegate，工程可编译通过</done>
</task>

</tasks>

<verification>
- Z_PasteApp.swift 存在且包含 @main 与 @NSApplicationDelegateAdaptor
- main.swift 不再作为入口参与编译
- xcodebuild 构建成功
</verification>

<success_criteria>
- 入口文件恢复并满足验证缺口描述
- Key link: Z_PasteApp.swift -> AppDelegate.swift 通过 @NSApplicationDelegateAdaptor 成立
- 构建输出包含 BUILD SUCCEEDED
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-06-SUMMARY.md`
</output>
