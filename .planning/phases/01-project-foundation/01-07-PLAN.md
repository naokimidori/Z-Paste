---
phase: 01-project-foundation
plan: 07
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - Z-Paste/Services/ClipboardService.swift
autonomous: true
requirements: ["FR-1.2", "NFR-1"]
must_haves:
  truths:
    - "当剪贴板图片数据超过 5MB 时，仅保存缩略图数据而非原始大图"
    - "当剪贴板图片数据不超过 5MB 时，仍保存原始图片数据"
  artifacts:
    - path: "Z-Paste/Services/ClipboardService.swift"
      provides: "图片大小阈值判断与缩略图生成逻辑"
      contains: "maxImageBytes"
  key_links:
    - from: "Z-Paste/Services/ClipboardService.swift"
      to: "缩略图生成逻辑"
      via: "extractContent 图片分支"
      pattern: "extractContent\(\).*maxImageBytes"
---

<objective>
补齐 ClipboardService 的大图降级策略：图片数据超过 5MB 时生成并保存缩略图，确保符合内存与存储约束。

Purpose: 满足 Phase 01 对图片数据大小的限制要求，避免大图占用过多内存与数据库空间。
Output: ClipboardService 图片处理逻辑完善（阈值判断 + 缩略图生成）。
</objective>

<execution_context>
@/Users/longzhao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/longzhao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@Z-Paste/Services/ClipboardService.swift

<interfaces>
From Z-Paste/Services/ClipboardService.swift:
```swift
class ClipboardService {
    private func extractContent() -> ClipboardItem?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 ClipboardService 实现 >5MB 图片缩略图降级</name>
  <read_first>
    - Z-Paste/Services/ClipboardService.swift
    - .planning/phases/01-project-foundation/01-VERIFICATION.md
  </read_first>
  <files> Z-Paste/Services/ClipboardService.swift </files>
  <action>
    在 ClipboardService 内新增可复用的图片大小阈值常量（5 * 1024 * 1024）。
    为 .png/.tiff 分支加入判断：当 imageData/tiffData 大于阈值时，使用 AppKit 生成保持宽高比的缩略图（例如以最大边限制为 512px，可根据当前项目风格自定但必须保持比例），并把缩略图数据写入 ClipboardItem.data；当不超过阈值时继续保存原始数据。
    缩略图生成需基于现有类型（NSImage/NSBitmapImageRep），避免引入新依赖；同时确保生成失败时有兜底（例如回退到原始数据或返回 nil，需与现有错误策略一致）。
    更新 extractContent 的图片分支确保只存在一个保存入口，避免重复逻辑。
  </action>
  <verify>
    <automated>xcodebuild -project "/Users/longzhao/aicodes/Z-Paste/Z-Paste.xcodeproj" -scheme "Z-Paste" -destination 'platform=macOS' build</automated>
  </verify>
  <acceptance_criteria>
    - 当读取到 PNG/TIFF 图片数据且大小超过 5MB 时，ClipboardItem.data 为缩略图数据而非原始数据。
    - 当图片数据大小不超过 5MB 时，ClipboardItem.data 保持原始数据。
    - extractContent 内不再直接无条件保存原始图片数据。
  </acceptance_criteria>
  <done>
    ClipboardService 的图片分支包含阈值判断与缩略图生成逻辑，并通过构建验证。
  </done>
</task>

</tasks>

<verification>
构建通过，且 ClipboardService 图片分支具备 5MB 阈值判断与缩略图降级逻辑。
</verification>

<success_criteria>
- 5MB 以上图片仅保存缩略图数据
- 5MB 以下图片保存原始数据
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-07-SUMMARY.md`
</output>
